[package]
name = "public_role_grant"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = {workspace = true}

[lib]
crate-type = ["rlib"]

[features]
# Keep default empty; other fixtures rely on unit tests.
default = []

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
