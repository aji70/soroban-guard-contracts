[package]
name = "severity-reporter-trust"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract demonstrating unweighted severity trust from any scanner"
license = "MIT"
repository = "https://github.com/Mirabliss/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
