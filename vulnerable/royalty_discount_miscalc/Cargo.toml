[package]
name = "royalty-discount-miscalc"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract demonstrating royalty calculated on post-discount price instead of sale price"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
